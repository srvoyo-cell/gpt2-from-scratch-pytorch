# GPT-2 from Scratch (PyTorch)

Учебная реализация архитектуры GPT-2 с нуля на PyTorch без использования готовых моделей и библиотек с предобученными весами.

Проект создан с целью глубокого понимания архитектуры Transformer, механизма self-attention и принципов работы autoregressive language models.

Реализация выполнена по мотивам и с опорой на материалы книги Себастьяна Рашки, с самостоятельной реализацией всех ключевых компонентов.

---

## 🔍 Описание проекта

В рамках проекта реализована архитектура GPT-2 на базовом уровне, включая все ключевые компоненты Transformer:

* embedding слои
* positional encoding
* multi-head self-attention
* feed-forward блоки
* layer normalization
* causal masking
* autoregressive генерация текста

Проект ориентирован на обучение и исследование, а не на достижение state-of-the-art качества.

---

## 🧠 Архитектура модели

Модель реализована модульно для максимальной прозрачности и удобства изучения.

Основные компоненты:

* **GPT2Model** — основная модель
* **TransformerBlock** — Transformer-блок
* **MultiHeadAttention** — механизм multi-head self-attention с causal mask
* **FeedForward** — позиционно-независимая feed-forward сеть
* **LayerNorm** — layer normalization
* **GELU** — функция активации

Каждый компонент вынесен в отдельный файл и реализован вручную.

---

## 📁 Структура проекта

```
GPT2/
├── data/                    # Данные для обучения
├── cfg_gpt2.py              # Конфигурация модели
├── gpt2.py                  # Основная модель GPT-2
├── transformer_block.py     # Transformer-блок
├── multiheadattention.py    # Multi-head self-attention
├── feedforward.py           # Feed-forward сеть
├── layer_norm.py            # Layer normalization
├── gelu.py                  # GELU активация
├── tokenizer.py             # Токенизация
├── generation.py            # Генерация текста
├── load_data.py             # Загрузка и подготовка данных
├── loss.py                  # Функция потерь
├── trainer.py               # Цикл обучения
├── main.py                  # Точка входа
└── README.md
```

---

## 🚀 Обучение модели

Для запуска обучения выполните:

```bash
python main.py
```

Основные параметры модели и обучения настраиваются в файле `cfg_gpt2.py`.

---

## ✍️ Генерация текста

После обучения модель поддерживает autoregressive генерацию текста с использованием causal masking.

Процесс генерации реализован в файле `generation.py`.

---

## 🛠 Используемые технологии

* Python
* PyTorch
* Transformer architecture
* Self-Attention
* Autoregressive language modeling
* NLP

---

## 🎯 Цели проекта

* Глубокое понимание архитектуры GPT-2
* Практическая реализация Transformer-модели с нуля
* Изучение механизмов self-attention и causal masking
* Получение опыта разработки DL-моделей без использования готовых решений

---

## 📌 Статус проекта

Проект является учебным и исследовательским.

Возможные направления развития:

* обучение на более крупных датасетах
* добавление поддержки pretrained весов
* обучние ассистента на соответсвующих размеченых данных
* расширение возможностей генерации текста

---

Проект разработан в учебных и исследовательских целях.
